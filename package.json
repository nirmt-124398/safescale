{
  "name": "safescale",
  "version": "0.1.0",
  "private": true,
  "description": "SafeScale Phase 1 proxy layer",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "node --test"
  },
  
  "dependencies": {
    "fastify": "^5.3.2"
  }
}